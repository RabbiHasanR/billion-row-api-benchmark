# pool_hba.conf for Pgpool-II
# Format is similar to PostgreSQL’s pg_hba.conf.
# This sample allows all clients (adjust as needed for production).

host    all         all         0.0.0.0/0         md5
